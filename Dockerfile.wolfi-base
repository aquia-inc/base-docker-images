FROM --platform=linux/amd64 cgr.dev/chainguard/wolfi-base:latest

USER root

# if you need a quicker turnaround, you can patch the image like the below, before the automated image build picks up the new CVE fixes
# RUN apk update && \
# apk add --upgrade --no-cache libssl3 libcrypto3
USER nonroot
